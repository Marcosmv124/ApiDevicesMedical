2. Ver qué archivos cambiaron (opcional):
   git status

3. Agregar todos los cambios al commit:
   git add .

4. Hacer el commit con un mensaje:
   git commit -m "Descripción de tus cambios"

5. Subir los cambios a GitHub:
   git push origin main

   2. Ver qué archivos cambiaron (opcional):
   git status

3. Agregar todos los cambios al commit:
   git add .

4. Hacer el commit con un mensaje:
   git commit -m "Descripción de tus cambios"

5. Subir los cambios a GitHub:
   git push origin main

6. Bajar cambios desde GitHub (actualizar tu repositorio local):
   git pull origin main
